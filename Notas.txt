
->CRIAR A PASTA PARTILHADA
mkdir mount

-> INSTALAR PACOTES DE PARTILHA NA VIRTUAL BOX
apt-get install virtualbox-guest-additions-iso
sudo reboot now

-> INSTALAR PACOTES DE PARTILHA NA VIRTUAL BOX
sudo apt install build-essential dkms
sudo reboot now


-> MOUNT A PASTA /mnt
sudo mount /dev/cdrom /mnt
cd /mnt
sudo ./VBoxLinuxAdditions.run 
sudo reboot now 



-> COLOCAR NO GRUPO VBOXSF O USER RSILVA-E
sudo usermod -aG vboxsf rsilva-e


-> COLOCAR O SSH DA MAQUINA NA MAQUINA VIRTUAL PARA DAR PREMISSAO:
vim ../.ssh/authorized_keys 
export XAUTHORITY=~/.Xauthority

->INSTALAR O FIREFOX
sudo snap install firefox


-> INSTALAR O DOCKER 
sudo snap install docker
sudo addgroup --system docker
